// TODO: Write code to define and export the Employee class
// lines (3 to 9) class Employee given by Dan M
class Employee {
  constructor(name, email, role) {
    this.name = name;
    this.email = email;
    this.role = role;
  }
}

// methods

getName();

getId();

getEmail();

getRole();

module.exports = Employee;
